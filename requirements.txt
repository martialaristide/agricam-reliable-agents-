# Cœur du projet (modèles, harnais, vérificateur, sécurité) : aucune
# dépendance externe autre que la bibliothèque standard Python 3.11+.

# Agent réel (agent/llm_client.py, AnthropicLLMClient) :
anthropic>=0.40.0

# Développement / tests
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.6.0

# Dashboard (phase 5, non couvert par ce paquet)
streamlit>=1.38.0
plotly>=5.24.0
